
"use client"

import * as React from "react"

export default function SettingsPage() {
  return (
    <div className="space-y-6 animate-in fade-in duration-500">
      <div className="flex flex-col gap-2">
        <h2 className="text-3xl font-bold tracking-tight uppercase text-primary">CONFIGURAÇÃO</h2>
        <p className="text-muted-foreground uppercase text-sm">GERENCIE AS DEFINIÇÕES GERAIS DO SISTEMA.</p>
      </div>
      
      <div className="rounded-lg border border-dashed p-12 flex items-center justify-center bg-muted/30">
        <p className="text-muted-foreground uppercase text-xs italic">ESTA PÁGINA ESTÁ VAZIA E PRONTA PARA RECEBER NOVAS FUNCIONALIDADES.</p>
      </div>
    </div>
  )
}
